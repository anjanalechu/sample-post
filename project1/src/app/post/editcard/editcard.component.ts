import { Component } from '@angular/core';

@Component({
  selector: 'app-editcard',
  templateUrl: './editcard.component.html',
  styleUrls: ['./editcard.component.css']
})
export class EditcardComponent {
  // datas = []; // Assume this is populated with your data
  // isEditing = false;
  // selectedPost: any;

  // editPost(post: any) {
  //   this.selectedPost = { ...post }; // Clone the post data
  //   this.isEditing = true;
  // }

  // deletePost(postId: number) {
  //   this.datas = this.datas.filter(post => post.id !== postId);
  //   // Optionally, add logic to remove the post from the server
  // }

  // updatePost() {
  //   const index = this.datas.findIndex(post => post.id === this.selectedPost.id);
  //   if (index !== -1) {
  //     this.datas[index] = { ...this.selectedPost }; // Update the post in the array
  //     this.isEditing = false; // Close the edit form
  //   }



}
